import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginLeft: 5,
  },
  starIcon: {
    fontSize: 16,
    color: 'grey'
  }
});

export default styles;
